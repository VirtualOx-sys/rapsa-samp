<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>R.A.P.S.A - Sistema de Antecedentes Penales</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    body {
      background-color: #1a1a1a;
      min-height: 100vh;
      height: auto;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 15px;
    }
    .login-container {
      text-align: center;
      color: white;
      padding: 2rem;
      border-radius: 10px;
      background-color: #2c2c2c;
      box-shadow: 0 0 20px rgba(0,0,0,0.5);
      width: 100%;
      max-width: 400px;
      margin: 15px auto;
    }
    .login-btn {
      background-color: #7289da;
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
      color: white;
      font-size: 1.1rem;
      transition: all 0.3s ease;
    }
    .login-btn:hover {
      background-color: #5b6eae;
      transform: translateY(-2px);
    }
    .logo {
      width: 120px;
      max-width: 80%;
      height: auto;
      margin-bottom: 1rem;
    }
    @media (max-width: 480px) {
      .login-container {
        padding: 1.5rem;
      }
      h1 {
        font-size: 1.5rem;
      }
    }
  </style>
</head>
<body>
  <% if (!user) { %>
    <div class="login-container">
      <img src="/img/logo.png" alt="RAPSA Logo" class="logo">
      <h1 class="mb-4">R.A.P.S.A</h1>
      <p class="mb-4">Sistema de Antecedentes Penales SAPD</p>
      <a href="/login" class="login-btn">
        <i class="fab fa-discord me-2"></i>Iniciar Sesión con Discord
      </a>

      <% if (error) { %>
        <div class="alert alert-danger mt-3">
          No tienes acceso a este recurso
        </div>
      <% } %>
      
      <% if (locals.error) { %>
        <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
        <script>
          document.addEventListener('DOMContentLoaded', function() {
            Swal.fire({
              icon: 'error',
              title: 'Error de Acceso',
              text: '<%= locals.message %>',
              confirmButtonColor: '#d33'
            });
          });
        </script>
      <% } %>
    </div>
  <% } else { %>
    <script>window.location.href = "/antecedentes/listar";</script>
  <% } %>
</body>
</html>